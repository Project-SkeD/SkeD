from django.shortcuts import render

def findSkill(request):
    """Renders the Find Skill page."""
    assert isinstance(request, HttpRequest)
    return render(
        request,
        'app/index.html',
        {
            'title':'Home Page',
            'year':datetime.now().year,
        }
    )